<%= form_with(model: image_state, local: true) do |form| %>
  <% if image_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_state.errors.count, "error") %> prohibited this image_state from being saved:</h2>

      <ul>
      <% image_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
